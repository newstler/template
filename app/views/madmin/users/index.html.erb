<%= content_for :title, resource_name_plural %>

<header class="header">
  <h1><%= resource_name_plural %></h1>
</header>

<div class="filters mb-4 p-4 card-bg">
  <%= form_with url: resource.index_path, method: :get, class: "flex gap-3 items-end flex-wrap" do |f| %>
    <div>
      <%= f.label :q, t("madmin.common.search"), class: "block text-sm font-medium text-dark-300 mb-1" %>
      <%= f.search_field :q, value: params[:q], placeholder: t(".search_placeholder"), class: "form-input" %>
    </div>

    <div>
      <%= f.label :created_at_from, t("madmin.common.from"), class: "block text-sm font-medium text-dark-300 mb-1" %>
      <%= f.date_field :created_at_from, value: params[:created_at_from], class: "form-input" %>
    </div>

    <div>
      <%= f.label :created_at_to, t("madmin.common.to"), class: "block text-sm font-medium text-dark-300 mb-1" %>
      <%= f.date_field :created_at_to, value: params[:created_at_to], class: "form-input" %>
    </div>

    <div class="flex gap-2">
      <%= f.submit t("madmin.common.filter"), class: "btn btn-primary" %>
      <% if params[:q].present? || params[:created_at_from].present? || params[:created_at_to].present? %>
        <%= link_to t("madmin.common.clear"), resource.index_path, class: "btn btn-secondary" %>
      <% end %>
    </div>
  <% end %>
</div>

<div class="table-scroll">
  <table>
    <thead>
      <tr>
        <th><%= sortable :name, t(".col_user") %></th>
        <th><%= sortable :teams_count, t(".col_teams") %></th>
        <th><%= sortable :chats_count, t(".col_chats") %></th>
        <th><%= sortable :total_cost, t(".col_cost") %></th>
        <th><%= sortable :created_at, t(".col_joined") %></th>
      </tr>
    </thead>

    <tbody>
      <% @records.each do |record| %>
        <tr>
          <td>
            <%= link_to resource.show_path(record), class: "flex items-center gap-3" do %>
              <img src="https://www.gravatar.com/avatar/<%= Digest::MD5.hexdigest(record.email.downcase.strip) %>?s=32&d=mp" class="w-8 h-8 rounded-full" alt="">
              <div>
                <div class="font-medium"><%= record.name.presence || t("madmin.common.unnamed") %></div>
                <div class="text-xs text-dark-500"><%= record.email %></div>
              </div>
            <% end %>
          </td>
          <td>
            <div class="flex flex-wrap gap-1">
              <% record.memberships.each do |membership| %>
                <% role_class = case membership.role
                  when "owner" then "bg-purple-500/15 text-purple-400"
                  when "admin" then "bg-blue-500/15 text-blue-400"
                  else "bg-dark-700 text-dark-400"
                end %>
                <%= link_to "/madmin/teams/#{membership.team.slug}", class: "inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-xs font-medium #{role_class}" do %>
                  <span><%= membership.team.name %></span>
                  <% if membership.role != "member" %>
                    <span class="opacity-60"><%= membership.role %></span>
                  <% end %>
                <% end %>
              <% end %>
              <% if record.memberships.empty? %>
                <span class="text-xs"><%= t(".no_teams") %></span>
              <% end %>
            </div>
          </td>
          <td><%= record.chats.size %></td>
          <td class="<%= record.total_cost >= 1.0 ? 'text-red-400' : 'text-emerald-400' %>"><%= format_cost(record.total_cost || 0, precision: 4) %></td>
          <td class="text-sm"><%= l(record.created_at, format: :short) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<div class="pagination">
  <% if @pagy.respond_to?(:series_nav) %>
    <%== @pagy.series_nav if @pagy.last > 1 %>
    <%== @pagy.info_tag %>
  <% else %>
    <%== pagy_nav @pagy if @pagy.last > 1 %>
    <%== pagy_info @pagy %>
  <% end %>
</div>
