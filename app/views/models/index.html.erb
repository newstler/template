<p style="color: green"><%= notice %></p>

<% content_for :title, "Models" %>

<h1>Models</h1>

<% if current_admin %>
  <p>
    <%= button_to "Refresh Models", models_refresh_path, method: :post %>
  </p>
<% end %>

<div id="models">
  <table>
    <thead>
      <tr>
        <th>Provider</th>
        <th>Model</th>
        <th>Context Window</th>
        <th>$/1M tokens (In/Out)</th>
        <th colspan="2"></th>
      </tr>
    </thead>
    <tbody>
      <%= render @models %>
    </tbody>
  </table>
</div>

<%= link_to "Back to chats", chats_path %>