<!DOCTYPE html>
<html class="dark">
  <head>
    <title><%= content_for(:title) || "Template" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#0f172a">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" type="image/png" href="/favicon-96x96.png" sizes="96x96" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="shortcut icon" href="/favicon.ico" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <meta name="apple-mobile-web-app-title" content="Template" />
    <link rel="manifest" href="/site.webmanifest" />

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="body-bg" data-controller="sidebar">
    <%= render "shared/flash" %>

    <% if current_user %>
      <%# Sidebar Navigation %>
      <%= render "shared/sidebar" %>

      <%# Mobile header %>
      <header class="lg:hidden fixed top-0 left-0 right-0 z-20 h-16 bg-dark-900 border-b border-dark-700 flex items-center px-4">
        <button type="button"
                data-action="click->sidebar#toggle"
                class="p-2 -ml-2 text-dark-400 hover:text-dark-100 hover:bg-dark-800 rounded-lg transition-colors">
          <%= inline_svg "icons/menu.svg", class: "w-6 h-6" %>
        </button>
        <span class="ml-3 font-semibold text-white"><%= content_for(:title) || "Template" %></span>
      </header>

      <%# Main content area %>
      <div class="transition-all duration-300" data-sidebar-target="mainContent">
        <%# Spacer for mobile header %>
        <div class="h-16 lg:hidden"></div>

        <%# Page content %>
        <main class="<%= content_for?(:full_width) ? '' : 'p-4 lg:p-8' %>">
          <%= yield %>
        </main>
      </div>
    <% else %>
      <%# Unauthenticated layout (login pages, etc.) %>
      <main class="min-h-screen flex items-center justify-center p-4">
        <%= yield %>
      </main>
    <% end %>
  </body>
</html>
