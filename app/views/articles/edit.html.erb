<% content_for :title, t(".heading") %>

<div class="max-w-7xl mx-auto px-4 py-8">
  <h1 class="text-3xl font-bold text-white mb-8"><%= t(".heading") %></h1>
  <%= render "form", article: @article, form_url: team_article_path(current_team, @article) %>
</div>
